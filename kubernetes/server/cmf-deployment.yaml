apiVersion: apps/v1
kind: Deployment
metadata:
  name: cmf-server-deployment
  labels:
    app: cmf-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cmf-server
  template:
    metadata:
      labels:
        app: cmf-server
    spec:
      containers:
      - name: cmf-server
        image: cmf-server:latest
        imagePullPolicy: IfNotPresent   # important for Minikube local builds
        ports:
        - containerPort: 8080
        env:
        - name: POSTGRES_HOST
          value: "postgres"           # Kubernetes Service for Postgres
        - name: POSTGRES_USER
          value: "myuser"
        - name: POSTGRES_PASSWORD
          value: "mypassword"
        - name: POSTGRES_DB
          value: "mlmd"
        - name: POSTGRES_PORT
          value: "5432"
        - name: REACT_APP_CMF_API_URL
          value: "http://nginx"       # for internal reference, safe to keep
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 5
        volumeMounts:
        - mountPath: /cmf-server/data
          name: server-data
        - mountPath: /cmf-server/data/env
          name: server-env
        - mountPath: /cmf-server/data/static
          name: server-static
        - mountPath: /cmf-server/data/labels
          name: server-labels
        - mountPath: /cmf-server/data/tensorboard-logs
          name: tensorboard-logs
      volumes:
      - name: server-data
        persistentVolumeClaim:
          claimName: pvc-server-data
      - name: server-env
        persistentVolumeClaim:
          claimName: pvc-server-data-env
      - name: server-static
        persistentVolumeClaim:
          claimName: pvc-server-data-static
      - name: server-labels
        persistentVolumeClaim:
          claimName: pvc-server-data-labels
      - name: tensorboard-logs
        persistentVolumeClaim:
          claimName: pvc-tensorboard-logs
---
apiVersion: v1
kind: Service
metadata:
  name: server             # 👈 matches your nginx.conf (proxy_pass http://server:8080/)
  labels:
    app: cmf-server
spec:
  type: ClusterIP
  selector:
    app: cmf-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
