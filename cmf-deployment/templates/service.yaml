apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmf-deployment.fullname" . }}
  labels:
    {{- include "cmf-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cmf-deployment.selectorLabels" . | nindent 4 }}
